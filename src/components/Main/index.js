import React from 'react';
import { WingBlank } from 'antd-mobile';

const Index = (props) => {
  return (
    <WingBlank className="bxs-main">{props.children}</WingBlank>
  )
}

export default Index;